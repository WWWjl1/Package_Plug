# 芯片封装学习插件 · 待办任务表

> **维护约定**：每完成一项，我把 `- [ ]` 改成 `- [x]`，并在最底部「完成记录」里加一行。
> 你只需要看勾。想让我做哪一项，直接说编号（例如「做 A1-1」）。

**最后更新**：2026-09-18 · 版本 `0.4.0` · 课程已全亮（19 模块 / 114 知识点 / 450 题）

---

## ⚙️ 改完之后怎么生效（重要）

插件已经是常驻版，改动不用重新安装，但**生效方式分两种**：

| 改的是哪个文件 | 怎么生效 |
|---|---|
| `lib/client.js`（界面） | 在 dsh 页面 **Ctrl + Shift + R** 硬刷新 |
| `lib/index.js`（主机 / 读盘 / 自检逻辑） | **重启 dsh**（主机半是启动时加载进内存的，改磁盘不会重载） |
| `content/` `assets/`（课程内容） | 刷新页面即可（每次打开学习界面都会重新读） |
| `cordis.patch.yml` `package.json`（挂载与包声明） | **重启 dsh** |

---

## 进度总览

| 分类 | 完成 / 总数 |
|---|---|
| A. 内容铺设 | **20 / 20** |
| B. 界面与交互 | **7 / 7** |
| C. 健壮性 | **5 / 5** |
| D. 工程与仓库 | **5 / 7** |
| E. 可选增强 | **5 / 5** |
| **合计** | **42 / 44** |

---

## ✅ 已完成（基线）

插件本体与第一道工序已经可用了：

- [x] 常驻 web 插件：随 dsh 启动自动加载，**不需要重启后重新激活，也不需要点授权**
- [x] 数据目录**自定位**（`import.meta.url`），别人 clone 到任何位置都能用；可用 `cordis.patch.yml` 的 `config.root` 覆盖
- [x] 主机半：HTTP 路由 `/package-learn/api` + 9 个方法（读课程 / 存进度 / 记小测 / 管会话 / 课程自检）
- [x] 客户端半：侧栏常驻入口、学习界面（工序按钮 / 知识树 / 正文 / 剖面图 / 小测 / 问 AI）
- [x] 发送消息后**自动跳到 dsh 原生「对话」界面**看答案
- [x] 学习进度、小测流水、错题本**落盘持久化**
- [x] 侧栏入口：优先回到固定学习会话，不存在才新建
- [x] 桌面紧急卸载脚本（`紧急-卸载芯片封装学习插件.bat`）
- [x] 仓库入库 + README + TODO + LICENSE + 数据结构文档，个人进度由 `.gitignore` 排除

---

## A. 内容铺设（价值最大，也最花时间）

> 每道工序 = 5~6 个细分点 + 1 张剖面/流程示意图 + 每点 3 道小测题。
> **建议每做完一道就让你试读**，确认深浅合适了再铺下一道。

### A1. 传统封装 8 道工序

- [x] **A1-4 引线键合**（6 细分点 / 18 题 / 1 图）← 已完成，作为其余工序的模板
- [x] **A1-1 晶圆减薄** — 已完成（深度版）：6 个细分点 / 24 道小测题 / 1 张剖面图。覆盖「为什么减薄（刚度∝t³）」「磨削与脆性—延性转变」「亚表面损伤与应力释放」「Taiko·DBG·临时键合」「典型缺陷」「TTV·Bow·Warp 测量」，小测按**面试追问**的难度设计
- [x] **A1-2 划片** — 6 细分点 / 24 题 / 1 图（刀片刚度 ∝ t³、线速度 v=πDn/60、σ∝1/√a、划片道宽度加和公式、DBG 与隐形切割）
- [x] **A1-3 装片** — 6 细分点 / 24 题 / 1 图（装片层热阻 0.33 K/W vs 硅本体 0.027、六种材料路线、空洞率对热阻的放大、SAM vs X-ray 盲区）
- [x] **A1-5 塑封** — 6 细分点 / 24 题 / 1 图（EMC 填料占比与 CTE、传递注塑、冲丝与弧高、MSL 与爆米花效应、260°C 水汽压强 4.7 MPa）
- [x] **A1-6 植球** — 6 细分点 / 24 题 / 1 图（面阵引脚数 (L/p)²、钢网面积比 AR、回流四段曲线、共面性预算、HIP 与 Kirkendall）
- [x] **A1-7 切筋成型** — 6 细分点 / 24 题 / 1 图（冲切间隙 5~10% 料厚、单根冲切力 12 N、ε=t/(2R+t) 与回弹、毛刺判据、共面性三点定基准）
- [x] **A1-8 测试** — 6 细分点 / 24 题 / 1 图（CP/FT/SLT/Burn-in 分级、Cpk 与 DPPM、Arrhenius 加速因子实算 78 倍、浴盆曲线、测试成本经济学）

### A2. 先进封装 6 个模块（晶圆级流程，顺序与传统完全不同）

- [x] **A2-1 Bumping 凸点** — 6 细分点 / 24 题 / 1 图（I/O 倍数 L/4p、金丝 0.1Ω vs Cu 柱 0.7mΩ、UBM 三层为何缺一不可、电流密度与电迁移阈值）
- [x] **A2-2 WLCSP** — 6 细分点 / 25 题 / 1 图（RDL 重布线、晶圆级 vs 芯片级顺序差异、CTE 失配与 DNP 距离——为什么芯片越大越不可靠）
- [x] **A2-3 Fan-out 扇出** — 6 细分点 / 24 题 / 1 图（引脚数 N≈((L−2m)/p+1)²、塑封重构与 KGD、**die shift δ=r·θ 角上 12.4μm vs 余量 10μm**、翘曲与芯片占空比）
- [x] **A2-4 TSV 硅通孔** — 6 细分点 / 24 题 / 1 图（**有效 AR：名义 10 实为 12.3**、Bosch 扇贝纹、电镀 bottom-up 填充、**KOZ 禁布区 2~3 倍直径**、Cu 突出百纳米来源）
- [x] **A2-5 2.5D / 3D 堆叠** — 6 细分点 / 24 题 / 1 图（堆叠良率相乘、KGD 成本模型、HBM 带宽=位宽×频率、**热阻拆解：硅只占 4.9%，BEOL+界面占 95%**）
- [x] **A2-6 Chiplet 芯粒** — 6 细分点 / 24 题 / 1 图（**良率 Y=exp(−D₀·A) 超线性实算**、光罩 858mm² 硬约束、D2D 能效阶梯 pJ/bit、异构集成的节点选择盈亏平衡点）

### A3. 专题模块 5 个（横着切，按知识类别）

- [x] **A3-1 封装形式分类** — 6 细分点 / 24 题 / 1 图（周边 4L/p vs 面阵 (L/p)²、BGA 逃逸布线反推 PCB 层数、QFN 散热焊盘、CSP 1.2 倍判据及其边界）
- [x] **A3-2 封装材料** — 6 细分点 / 24 题 / 1 图（CTE 协同第一性原理、ΔL=Δα·ΔT·L 实算、导热阶梯、IMC 的 √(k·t) 与 Arrhenius、五种失效模式）
- [x] **A3-3 设备** — 6 细分点 / 24 题 / 1 图（**整线 UPH 表：键合 1875 是瓶颈、前道 22ms vs 键合 1.92s 差 90 倍**、耗材经济学、四种检测手段的能力边界）
- [x] **A3-4 可靠性测试** — 6 细分点 / 24 题 / 1 图（Arrhenius AF 实算、**扩散冻结：−55°C 放 1000h ≡ 150°C 下 4 毫秒**、Coffin-Manson、Peck 湿度项、零失效抽样 76 FIT）
- [x] **A3-5 行业术语** — 6 细分点 / 24 题 / 1 图（三组易混词辨析表、互连术语体系、同义词陷阱、标准归属、产线口语；小测全部考辨析）

### A5. 跨模块串联（知识点互链）

- [x] **A5 相关知识点跳转** — 你选的方案：**做跳转，不做进度合并**。同一个概念在别的模块从别的角度讲过时，知识点正文下方多出一行「这个概念在别的模块也讲过：」，点一下直接跳过去。
  - 为什么不做进度合并：`wb.materials`（键合里的紫斑 / Kirkendall）、`mt.imc`（材料学的 √(k·t)）、`rl.hts`（150 °C 长烤的 Arrhenius AF）**讲的根本不是同一件事**。共用一个 id 会让「学过紫斑」被当成「学过 IMC 动力学」，进度是假的。CTE 失配、共面性、MSL 爆米花、浴盆曲线、DNP 同理。
  - 落地：**65 个知识点带关联，共 128 条链接，其中跨模块 121 条**；划片（`dc.*`）此前一条关联都没有，已专门补出出边与入边，不再是孤岛。
  - 数据侧：内容 JSON 里每个细分点多一个可选的 `related: [知识点 id]` 字段，纯加法，删掉也不影响其它功能。
  - 自检已扩展：`related` 指向不存在的 id、或指向自己，都会在学习界面「课程自检」里报出来（否则界面上那颗按钮会凭空消失，很难查）。

---

## B. 界面与交互（打磨）

- [x] **B1 「问 AI」快捷提问里的工序名写死了** — 已改为读取当前工序标题，铺别的工序后不会再问错对象
- [x] **B2 正文底部留白是估算值** — 改为**固定预留**：以「输入框空着/单行时的高度」为基准再加两行的高度（约 3 行输入的空间）。效果：输入框在一行到三行之间怎么变，**正文和知识树都纹丝不动、也永不被盖住**；知识树的可用高度同步扣掉这块预留
- [x] **B3 知识树的 sticky 偏移是估算值** — 顶部栏现在通过 ref 上报自己的真实高度，知识树的吸附位置与最大高度都跟着它走
- [x] **B4 错题本没有界面入口** — 顶栏主线那一行新增第四个入口 **「错题重做」**（排在「专题模块」后面），点进去是**整整一块独立界面**（不是浮层/下拉），**按知识点分组**：每个知识点一张卡片，卡片头是知识点名 + 来源工序 + 「去看这个知识点」；题内先选可改、提交后批改，**答对自动从错题本移出**
- [x] **B5 没有搜索** — 顶栏加了搜索框，同时搜**细分点标题 + 正文内容 + 小测题目**，命中处给上下文片段，点结果直接跳到那个知识点
- [x] **B6 缺少主线级总览** — 知识树里每条主线（传统封装 / 先进封装 / 专题模块）后面现在直接显示整条线的 `已掌握/总数`
- [x] **B7 小测改成「先选后交」** — （用户新增需求）原来点一下选项就立刻判分，没给人犹豫的时间。现在：点选项只**选中**（可随便改），每题一个**「提交本题」**按钮，提交后才批改并锁定、显示对错与解析；整份全对仍然自动标记已掌握

---

## C. 健壮性与错误处理

- [x] **C1 课程 JSON 自检** — 学习界面顶栏加了「课程自检」按钮：检查字段缺失 / 类型不对 / 配图找不到 / id 重复 / answer 越界 / 未登记的知识点 / 进度里的失效知识点，结果直接列出来
- [x] **C2 读盘失败没有重试入口** — 失败界面现在写明**失败原因 + 「课程数据在插件的 content/ 目录里，主机半没挂上或目录被移动都会这样」+ 一颗「重新读取」按钮**（清掉错误、重跑一遍 gate/load，不用刷新页面）
- [x] **C3 学习会话被删后不能自愈** — 两处一起修：
  - 入口按钮原来在 `sessions.open(id)` 抛错后**只是往控制台打一行**。现在：识别出是「未知 id」（会话被删）就先调 `forget-session` **把烂指针清掉**，再新建会话并把新 id 记回去。
  - 学习界面原来只会说「请点侧栏入口」，是个死胡同。现在那条提示旁边直接给一颗**「进入学习会话」按钮**，自己把会话建出来。
  - 两处共用同一份 `enterLearning()`，避免以后只修好其中一条路。
- [x] **C4 主机路由没有来源限制** — 加了来源判定，判据是 `Origin`：外域网页发来的请求直接 403（挡住**别的网页偷偷读写你的进度**，即 CSRF；DNS rebinding 同样会暴露外部 Origin，所以一并挡住）。不带 `Origin` 的调用一律放行，`Origin: null`、`dsh://` 这类自定义协议也放行。
  - **为什么不查 `Host`**：第一次实现时查了，后来自己撤掉了。`Host` 是请求方自己写的，本机程序随便伪造，**挡不住谁**；而桌面版 Electron 壳若用 `dsh://` `app://` 之类加载页面，一刀切回环检查会**把用户自己的界面也拦掉**。纯风险没收益。
  - **老实说**：这挡的是「浏览器里的别的网页」，本机其它程序仍可直接调用（彻底堵住需要鉴权，而密钥在浏览器里藏不住）。详见 README「已知限制」。
- [x] **C5 客户端服务名是运行时按名字取的** — 拿不到 `slots` 服务时不再静默退出：**限时重试 5 次**（每秒一次，应对加载顺序变化），仍拿不到就在**页面左下角显示一条红色提示**说明原因，控制台同时有 `package-learn:` 前缀的错误日志。`slots.inject` 登记槽位时抛错（槽位名被改）也走同一条提示。提示节点随插件一起销毁，不会残留。

---

## D. 工程与仓库

- [x] **D1 README 更新** — 已改成常驻版的正式安装说明（clone → 目录链接 → 改 profile → 重启），并标清阶段一为废弃
- [x] **D2 加 LICENSE 文件** — MIT，已加
- [ ] **D3 README 加截图 / GIF** — 现在只有 ASCII 示意图
- [x] **D4 处理 `plugin/` 目录** — 已在目录内和 README 里明确标注为「阶段一遗留 / 已废弃，仅作参考」
- [ ] **D5 客户端 bundle 引入构建步骤** — 现在是手写的 `__ModuleLoader__.load` 大文件（约 1920 行）。代码再长会难维护，可以考虑源文件 + 打包脚本
  > 我**不建议做**：现在「clone → 建目录链接 → 重启」就能跑，加了构建就变成「改了必须先 build」，和这条线的初衷（方便、可直接删除、不牵连别的东西）相冲。真要做的话，值得做的是把 `lib/client.js` 里那些纯逻辑（如 `reviewQueue`）拆成独立文件，纯前端仍在浏览器里拼装。
- [x] **D7 验收脚本收进仓库 + 渲染冒烟测试** — 以前 4 个验收脚本放在工作区、**不在版本控制里**（重下一次仓库就全没了）。现在收进 `tools/` 并改成自定位路径，一条命令跑完：`node tools/check-all.mjs`。
  - 新增 **`check-render.mjs`**：用自制迷你 React 把学习界面**真的渲染一遍**，再点几下按钮。它抓的是以前完全没覆盖的一类问题——渲染时崩溃、数据字段不对、**Hook 顺序变化**（早期那个 React #310）、某块界面根本没渲染出来、点了没反应。跑法：`content/` 拷到临时目录 + `config.root` 挂主机半，**不碰真实进度**。
  - 这套检查是**逐条验证过它真有用**的：CSS 同名选择器检查（把 `.pkl-bar` 冲突塞回去 → 报错）、横排溢出检查（去掉 `flex-wrap` → 报错）、渲染崩溃检查（渲染路径里注入异常 → 报错），每次验证后都核对文件哈希与注入前一致。
  - 顺手修掉两个检查自己的 bug：`check-content` 的 SVG 配对判据写错了（`open !== close + self`，自闭合标签本来就不需要闭合，导致每张图都误报），`check-client` 用正斜杠子串拼主机半路径（Windows 反斜杠替换不到，等于拿客户端半当主机半断言）。
- [x] **D6 版本号与 dsh 适配说明** — 版本升到 `0.2.0`；README 新增「**适配的 dsh 版本**」一节，写清验证环境（DSH Desktop `dsh-plugin-desktop 2.0.4` / `@deepseek-harness-tui/dsh-tui 0.10.0-beta.5`）和它**实际依赖的全部接口面**（2 个主机服务、3 个客户端服务、3 个槽位名、模块加载器、主题变量），以及名字对不上时去哪儿看提示、怎么临时停掉

---

## E. 可选增强（你没提过，供你挑）

- [x] **E1 学习进度导入 / 导出** — 顶栏新增 **「进度备份」** 面板：
  - **导出**：把 `state.json` + `quiz.json` + `ui.json` 打包成一个 JSON 文件下载（带格式标识 `dsh-package-learn/progress`）。**刻意不含 `session.json`**——会话 id 换台机器就是废的
  - **导入**：覆盖当前进度，但**覆盖前一定先把现有进度备份到 `progress/backups/<时间戳>/`**；**连备份都写不成功就拒绝导入**（绝不先删后写）。导错了把备份拷回去即可
  - **坏数据整包拒掉、一个字都不写盘**：空包 / 缺 quiz / 状态值非法 / 知识点不是对象 / 错题不是数组 五种情况各有明确理由
  - 导入成功后界面自动重读一遍进度（不用刷新页面）；`.gitignore` 已把 `progress/backups/` 一并排除
- [x] **E2 复习提醒** — 顶栏第五个入口 **「今日复习 N」**，点进去是独立界面（和错题重做同一套做法，不是浮层）。规则完全公开：
  - **有没消化的错题 → 立刻进队列，排最前**（错题是「没学会」的硬证据，比任何间隔都可信）；答对后自动移出
  - **在学且放了 3 天 → 催一下**。门槛为什么不是 1 天：打开一个点就自动标「在学」，所以「在学」只代表「点开过」——按 1 天算，实测本机 30 个在学点会一次性糊成一堵墙
  - **已掌握 → 间隔阶梯 1 / 3 / 7 / 16 / 35 天**，点「复习过了」往上走一档，35 天封顶
  - 每条写明**为什么现在该看它**（「错题还没消化（2 道）」／「已掌握，上次看是 9 天前，这一档间隔 3 天」），一次最多列 25 条，清掉上面的自动顶上
  - 算法抽成纯函数 `reviewQueue()`（不碰 React、不自取当前时间），验收脚本拿固定时间**真算一遍 17 条断言**；并用你本机真实进度跑过一次：队列 1 条，正是那道做错的键合原理题
  - 顺手修掉一个会悄悄吃数据的 bug：`set-status` 原来是**覆盖**写整个条目，会把复习记录抹掉。现在改成合并（客户端 `mark()`、自动标在学两处也一并改了），并加了回归测试
- [x] **E3 学习时长统计 / 时间线** — 顶栏新增 **「学习统计」**：今天 / 最近 7 天 / 累计 / 连续天数 / 有学习的天数 + 7 天柱状图 + 花时间最多的 5 个知识点。
  - **计时口径刻意保守**（界面上写着同一句话）：算——界面在前台且最近 5 分钟有鼠标/键盘/滚轮操作；不算——后台标签页、窗口最小化、超过 5 分钟没操作、切走时不足 5 秒的零头。**只在学习界面计时**，切到原生对话界面问 AI 的时间不算
  - **宁可少算，也不把「窗口挂了一夜」当成学习时长**——那种数字只会骗自己
  - 时长按**本地日期**分桶（不用 UTC，否则北京时间凌晨那几小时会被算到前一天）；单次心跳封顶 300 秒；只留最近 400 天
  - 连续天数：某天不足 1 分钟就断；今天还没学不算断签（从昨天起算）
  - 数据落在 `progress/time.json`，跟着 E1 的导出/导入一起走（旧导出件没有这个字段也能正常导入）；`.gitignore` 已排除
  - 算法抽成纯函数 `studySummary()` / `humanTime()` / `localDateKey()`，验收脚本拿固定时间算 17 条断言（含「凌晨 00:30 与 23:30 必须同属一天」这种专抓 UTC 用错的用例）
- [x] **E4 课程导出为 Markdown** — 「备份与导出」面板里第三颗按钮，把整份课程导成一个 `.md`：
  - **19 张图内嵌成 data URI**（默认）：导出来的是**单个文件就能用**，不用连带 `assets/` 一起拷，换台机器图也还在；也可传 `{inlineImages:false}` 退回相对路径写法（文件小得多）
  - **三级可跳转目录**：主线 / 模块 / 知识点都有锚点，264 个锚点零死链；锚点就是知识点 id，所以从别处也能用 `文件.md#wt.grind` 直接跳进来
  - **450 道题带正确选项与解析**；184 张表格结构完整；500 个重点/注意框转成引用块；正文里原来的 `**加粗**` 原样保留
  - **带上你的学习状态**（未学 / 在学 / 已掌握），所以这份导出件可以直接当**打印出来打勾的复习清单**
  - 验证：导出统计与课程完全一致（19 模块 / 114 知识点 / 450 题 / 19 图 / 128 条关联），全部由验收脚本核对（含「同一张表每行列数必须一致」这类渲染级检查）
- [x] **E5 设置界面** — 顶栏新增 **「设置」**：把平时藏在文件系统里的东西摊开 + 两个维护入口。
  - **显示**：插件版本、**数据目录绝对路径**、这个路径是自定位还是被 `config.root` 改过、主机半方法表、每个进度文件的大小、进度规模（多少知识点 / 错题 / 多少天时长）、已有多少份自动备份
  - **忘记学习会话**：会话被删、入口点不动时的手动出口（正常情况下 C3 会自动处理，这是备用手闸）
  - **清空学习进度**：清掉状态 / 错题 / 流水 / 时长（**不动学习会话与界面偏好**——清那两个没有意义）。不可逆操作，所以**必须二次确认**，且**服务端强制先备份**到 `progress/backups/<时间戳>-before-reset/`；连备份都写不成功就拒绝执行
  - **刻意偏离原条目**：原条目写的是「界面上改数据目录」，**没做**。改数据目录必须重启 dsh 才生效，做成按钮就是个「点了没反应」的半成品，还得把值写回 yml。现在只**显示路径 + 说明怎么改**
  - 测试抓到的一个真问题：`about()` 一开始从**数据目录**读 `package.json` 拿版本号——一旦用 `config.root` 把数据目录指到同步盘，那儿没有 `package.json`，版本就是空的。已改为从**插件包目录**读（版本描述的是插件，不该跟着数据走），并加了静态断言盯住

---

## 建议顺序

```
1. ✅ B1 + D1 + D2 + D4     零碎收尾（已完成）
2. ✅ C1 课程自检            铺内容之前先有工具（已完成）
3. A1-1 第一道工序          铺一道 → 你试读 → 定下深浅标准
4. A1 剩余 + 配图            按第 3 步的标准批量铺
5. A2 / A3                  先进封装 + 专题
6. B2~B6 / C2~C5 / D3 / D5/D6  体验与工程收尾
```

---

## 完成记录

| 日期 | 项 | 说明 |
|---|---|---|
| 2026-09-17 | 基线 | 阶段一动态插件完成；界面定稿（发送后自动跳转） |
| 2026-09-17 | 基线 | 上传 GitHub 仓库（源码 + 文档，排除个人进度） |
| 2026-09-17 | 基线 | 阶段二常驻 web 插件完成并装入 profile，路径改为自定位 |
| 2026-09-17 | 基线 | 修复入口点击无反应（客户端服务改为点击时懒解析） |
| 2026-09-17 | A1-4 | 引线键合：6 细分点 / 18 题 / 剖面图 |
| 2026-09-17 | B1 | 快捷提问的工序名改为动态读取 |
| 2026-09-17 | D1 | README 改写为常驻版安装说明 |
| 2026-09-17 | D2 | 加入 MIT LICENSE |
| 2026-09-17 | D4 | `plugin/` 标注为废弃 |
| 2026-09-17 | C1 | 课程自检：主机校验逻辑 + 界面「课程自检」按钮 |
| 2026-09-17 | B2 | 正文留白改为探针实测（对话框上方零高度探针） |
| 2026-09-17 | B3 | 知识树吸附偏移改为顶部栏实测高度 |
| 2026-09-17 | B6 | 知识树每条主线显示整条线的已掌握 / 总数 |
| 2026-09-17 | B7 | 小测改为先选后交：选中可改，每题单独提交批改（用户新增需求） |
| 2026-09-17 | B2 | 输入区预留改为**固定值**（1 行基准 + 两行余量），内容不再随输入框跳动、不再被盖住（用户返工要求） |
| 2026-09-18 | A1-1 | 晶圆减薄：6 细分点 / 24 题 / 剖面图（第 2 档） |
| 2026-09-18 | 标准 | 用户确认：剩余全部内容按**第 3 档（面试拷问级）**铺 |
| 2026-09-18 | A1-1 | 已提升到第 3 档：补入热阻公式 R=t/(kA)、TSV 深宽比计算、Griffith σ∝1/√a、刚度 t³ 量化、追问预判、多个失效链案例 |
| 2026-09-18 | A1-2~8 | 传统封装剩余六道工序一次铺完（划片 / 装片 / 塑封 / 植球 / 切筋成型 / 测试）：每道 6 细分点 / 24 题 / 1 图，全部按第 3 档深度 |
| 2026-09-18 | 整合① | 传统封装六道工序 status 翻 ready；知识点库登记 48 条；课程自检通过（已写工序 8 / 细分点 48 / 小测 186） |
| 2026-09-18 | A2-1~6 | 先进封装六个模块一次铺完（Bumping / WLCSP / Fan-out / TSV / 堆叠 / Chiplet）：每模块 6 细分点 / 24~25 题 / 1 图，全部第 3 档深度并做了跨模块引用 |
| 2026-09-18 | 整合② | 先进封装 status 翻 ready；知识点库累计 84 条；课程自检通过（已写工序 14 / 细分点 84 / 小测 330） |
| 2026-09-18 | A3-1~5 | 专题模块五个一次铺完（形式分类 / 材料 / 设备 / 可靠性 / 术语）：每模块 6 细分点 / 24 题 / 1 图 |
| 2026-09-18 | 整合③ | 专题 status 翻 ready；知识点库累计 114 条；课程自检通过（**已写工序 19 / 细分点 114 / 小测 450**）——**整个课程地图全亮** |
| 2026-09-17 | B4 | 错题重做改为独立界面（主线行第四个入口）+ 按知识点分组（用户返工要求） |
| 2026-09-17 | B5 | 全课程搜索：标题 / 正文 / 题目，带片段预览与跳转 |
| 2026-09-18 | A5 | 跨模块串联：知识点正文下方加「相关知识点」一行按钮，点击直接跳到那个模块的那个点（65 个知识点 / 128 条链接 / 跨模块 121 条）|
| 2026-09-18 | A5 | 划片不再是孤岛：`dc.*` 补出边 12 条 + 入边 4 条（减薄 / 切筋 / 设备耗材 / 板级可靠性四个方向双向打通）|
| 2026-09-18 | A5 | 高级封装断档补齐：bumping↔WLCSP↔fan-out↔stacking↔TSV↔chiplet 六个模块两两至少一条链接 |
| 2026-09-18 | C1 | 课程自检扩展：`related` 指向不存在的知识点、或指向自己，都会报出来（原来只静默少一颗按钮）|
| 2026-09-18 | 验收 | 新增整体验收脚本：假 ctx 真挂载主机半 → 走一遍 HTTP 路由 → 自检 + 断链检查 + 数据完整性，全部通过 |
| 2026-09-18 | C2 | 读盘失败界面：写明原因 + 数据目录提示 + 「重新读取」按钮（重跑 gate/load，不用刷新页面）|
| 2026-09-18 | C3 | 会话自愈：`open()` 遇到未知 id 时先 `forget-session` 清掉烂指针再新建；学习界面的死胡同换成「进入学习会话」按钮；两处共用 `enterLearning()` |
| 2026-09-18 | C4 | 路由来源限制：只查 `Origin`（外域 403，挡 CSRF 与 DNS rebinding），放行无 Origin / `null` / 自定义协议 |
| 2026-09-18 | C4 | 复查后**撤掉 Host 检查**：Host 可伪造、挡不住谁，却会误伤 `dsh://` 之类的宿主页面；验收脚本扩到 12 条来源用例（含 IPv6 回环、自定义协议、非法 Origin）|
| 2026-09-18 | C5 | 拿不到 `slots` 时限时重试 5 次，仍失败就在页面左下角显示红色提示（原来完全静默）；登记槽位抛错也走同一条提示 |
| 2026-09-18 | 自检 | 主机方法表扩到 10 个（新增 `forget-session`）；验收脚本扩到 10 组，含 7 条来源限制用例与临时目录上的 `config.root` 覆盖测试 |
| 2026-09-18 | D6 | 版本升到 `0.2.0`；README 写清验证环境与依赖的全部 dsh 接口面 |
| 2026-09-18 | 文档 | README 修正过期表述：状态改为「已完整可用」、仓库结构更新（19 模块 / 19 图 / 10 方法）、路线图三项内容铺满打勾 |
| 2026-09-18 | 文档 | README 改掉会误导人的「同一知识点用同一个 id 进度就互通」——那是 A5 明确否掉的方案，现改为「用 `related` 串联、不共用 id」并说明理由 |
| 2026-09-18 | E2 | 今日复习：顶栏第五个入口 + 独立界面；规则 = 错题未消立刻 / 在学放 3 天 / 已掌握按 1·3·7·16·35 天阶梯；每条写明「为什么现在该看它」|
| 2026-09-18 | E2 | 门槛从「在学 1 天」改成「在学 3 天」：实测本机 30 个点全在「在学」（打开就自动标），按 1 天算会一次糊成一堵墙 |
| 2026-09-18 | E2 | 复习算法抽成纯函数 `reviewQueue()`，验收脚本 17 条断言用固定时间真算一遍；另用本机真实进度跑过：队列 1 条，正是做错的那道键合原理题 |
| 2026-09-18 | 修 bug | `set-status` 原来覆盖写整个进度条目，会把复习记录抹掉；改成合并，客户端 `mark()` 与自动标在学两处同步修正，并加回归测试 |
| 2026-09-18 | 文档 | `progress/README.md` 补上复习规则的完整说明与「怎么重置某个点的复习节奏」；README 补「今日复习」一节 |
| 2026-09-18 | E1 | 进度备份面板：导出为 JSON（不含 session）+ 从文件导入；导入前强制自动备份，连备份失败都拒绝导入 |
| 2026-09-18 | E1 | 五种坏数据整包拒掉且不写盘；`.gitignore` 补 `progress/backups/`；主机方法表扩到 14 个 |
| 2026-09-18 | 验收 | 主机半新增 9c 组：导出→改动→导入的往返测试（含「备份里确实还有被覆盖掉的旧数据」）、五种坏包拒绝、拒绝时不写盘 |
| 2026-09-18 | E4 | 课程导出 Markdown：三级可跳转目录、450 题带答案与解析、19 张图内嵌成 data URI（单文件可用）、带上学习状态当打印清单 |
| 2026-09-18 | E4 | 面板从「进度备份」改名「备份与导出」，三颗按钮；主机方法表扩到 15 个（新增 export-markdown）|
| 2026-09-18 | 验收 | 新增第 11 组：264 个锚点零死链、184 张表格列数一致、答案标记与解析计数、内嵌图与相对路径两种模式、导出统计与课程逐项对齐 |
| 2026-09-18 | E3 | 学习统计：今天 / 7 天 / 累计 / 连续天数 / 有效天数 + 7 天柱状图 + 最花时间的知识点；计时口径保守（前台 + 5 分钟内有操作才算） |
| 2026-09-18 | E3 | 按本地日期分桶（不用 UTC）；单次心跳封顶 300 秒；只留最近 400 天；`time.json` 跟着 E1 导出导入一起走并被 `.gitignore` 排除 |
| 2026-09-18 | 验收 | 主机半新增 9d 组：心跳累加 / 单次封顶 / 本地日期分桶 / 五种非法入参拒绝 / 时长随导出导入往返 / 备份里有 time.json；客户端新增第 11 组 17 条断言 |
| 2026-09-18 | 自省 | 这几轮验收脚本抓到的问题里，有一半是**我自己写的测试期望错了**（条数写死、漏算一条数据、正则漏了行首缩进）——留着这些用例的价值就在这里：它逼着我逐条对着真实输出核一遍 |
| 2026-09-18 | E5 | 设置 / 关于：摊开插件版本、数据目录绝对路径与来源、方法表、各进度文件大小、进度规模、备份份数；外加「忘记学习会话」与「清空学习进度」两个入口 |
| 2026-09-18 | E5 | 清空进度必须二次确认 + 服务端强制先备份，备份失败就拒绝执行；只清状态/错题/流水/时长，不动学习会话与界面偏好 |
| 2026-09-18 | E5 | 刻意不做「界面里改数据目录」：改完必须重启才生效，做成按钮是半成品；改为显示路径 + 说明怎么改。版本号改从插件包目录读（测试发现 config.root 指到别处时版本会读空）|
| 2026-09-18 | 验收 | 主机半新增 9e 组：about 各项、不带 confirm 直接拒绝且不写盘、带 confirm 才清空、清空前的记录确实在备份里、学习会话没被误删 |
| 2026-09-18 | 版本 | `0.3.0` → `0.4.0`（E2~E5 四项新功能）；主机方法表 14 → 19 个 |
| 2026-09-18 | 小结 | **E 类做完（5/5）**。剩下 2 项：D3 要你给一张界面截图我才能写进 README；D5 我建议不做（理由见该条目）|
| 2026-09-18 | 事故 | **顶栏被压成 22px**：加学习统计柱状图时写了 `.pkl-bar{width:22px}`，可 `.pkl-bar` 早就是整个顶栏容器 —— 同名选择器后者覆盖前者，主线/搜索/工序按钮全被挤成竖排单字。柱状图四个类改名 `pkl-chart*` 修复 |
| 2026-09-18 | 事故 | 事后补的检查：**CSS 同名选择器被定义两次直接报错** + 结构容器各只能有一条规则 + 用到的类名必须有样式（后两条各验证过能拦住）|
| 2026-09-18 | 事故 | 同族隐患：`.pkl-toolbar` 没有 `flex-wrap`，窄窗口下按钮会溢出被裁、**用户根本不知道有这些功能**。改成允许换行 + `.pkl-track{flex:0 0 auto}` 永不压缩 |
| 2026-09-18 | D7 | 4 个验收脚本从工作区收进仓库 `tools/`（自定位路径），新增 `check-render.mjs` 渲染冒烟测试，`node tools/check-all.mjs` 一条命令跑完 |
| 2026-09-18 | D7 | 修掉两个检查脚本自己的 bug：SVG 配对判据写错（自闭合标签不用闭合，导致每张图误报）、用正斜杠子串拼路径（Windows 下替换不到，等于拿客户端半当主机半断言）|
| 2026-09-18 | 反思 | 这次事故暴露的真正缺口不是那个 bug，而是**我看不见界面**。补渲染测试比修 bug 本身更重要——而且每条新检查都验证过"它真的能拦住"，因为没验证过的回归测试等于没有测试 |
